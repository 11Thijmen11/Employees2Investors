# Employees2Investors — React + Tailwind demo

Korte demo-site voor `Employees2Investors`.

Run locally:

```powershell
npm install
npm run dev
```

Stack:
- React (Vite)
- Tailwind CSS
- Recharts

Notes:
- De top-banner `Beschikbare plekken: [x]` is dynamisch instelbaar in `src/App.jsx`.
- De grafiek laadt dummy data uit `src/data/prices.json`.
